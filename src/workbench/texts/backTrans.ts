const backTrans: Record<string, string> = {
  'Rom.5.3':
    'And not only in this, otherwise too in our sufferings, because we know that the suffering produces perseverance;',
};

export default backTrans;
